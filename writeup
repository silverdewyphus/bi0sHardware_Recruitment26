# MENU
PASSWORDS for each level are stored in /etc/somegame_pass/.


# LEVEL 0 -> LEVEL 1
 - list all files (combining -a -l -p -s gives info about the files in a neat manner by differentiating directories, and showing size)
 - `cat` the `readme` file
pswd: 6y2kwnwK6grgvwvpvLaa2T1cpFEKOhNR
timetaken: 9m


# LEVEL 1 -> LEVEL 2
 - we log into Level 1 using `ssh bandit1@bandit.labs.overthewire.org -p 2220` instead of `ssh bandit0@bandit.labs.overthewire.org -p 2220`
 - password is what we got in Level 0
 - can't just use `cat -` because it asks for more input
 - from a blog on Medium: The proper way to view content of dashed filename would be again to prefix the path of the file
 - use `cat ./-`
pswd: PK8fYLZg2hnHSz83plBL1iEPKdD3QToB
timetaken: 14m 6s


# LEVEL 2 -> LEVEL 3
 - since the filename has spaces, `cat` thinks each word is a different file (as explained by that one senior in the bi0s CTF stall)
 - from commandlinux.com: Use Quotation Marks Around the Filename
 - I tried using quotes but I think because of the `--` at the start it thinks `spaces` is a command, so like the previous level we can use `./` to provide location of the file
pswd: 7ZZ2LFrykP2zEyvBl4m3clcL7tGYJPME
timetaken: 12m 45s


# LEVEL 3 -> LEVEL 4
 - display all files
 - go to `inhere` directory using `cd ./inhere/`
 - `cat` the "...Hiding-From-You" file
pswd: xzTXq1rDJQVVAzdv5cHq1TQytTWufAMq
timetaken: 1m 42s


# LEVEL 4 -> LEVEL 5
 - run `ls --help` to see that human-readable files can be listed using `ls -h`
 - we get `-file00` to `-file09`
 - if we `cat` each one of them, we get the password in `-file07`
pswd: 6C7h9GD8M6ai5nr7wo1RonrzFjj9yIrG
tiemtaken: 13m 20s


# LEVEL 5 -> LEVEL 6
 - in manpages.ubuntu.com:  -type c
                            File is of type c

                            -size n[cwbkMG]
                            File uses less than, more than or exactly n units of space, rounding up.

                            -executable
                            Matches files which are executable

 - we use `find -type f -size 1033c ! -executable` to show FILES of size 1033 BYTES which are NOT EXECUTABLE
 - we get `./maybehere07/.file2`
 - if we `cat` it, we get the password
pswd: pXa26xhMWaC2SvDotA4r9EgZkulOeSBW
timetaken: 16m 44s


# LEVEL 6 -> LEVEL 7
 - "somewhere on the server" => have to search the root directory( / )
 - in manpages.ubuntu.com: Other primaries
                           The primaries -atime, -ctime, -depth, -exec, -group, -links, -mtime, -nogroup, -nouser, -ok, -path, -print, -prune, -size, -user and -xdev are all supported.
 - we can use `find / -type f -user bandit7 -group bandit6 -size 33c` to show FILES of size 33 BYTES which are owned by USER bandit7 and GROUP bandit6
 - we see a hugeee list of files with "Permission denied" but one file is named `bandit7.password` in address: /var/lib/dpkg/info/bandit7.password
 - we `cat` it.
pswd: Bmnnvf82KzQlfxgAI2d1zYbr1u9pr3E3
timetaken: 6m 23s


# LEVEL 7 -> LEVEL 8
 - we just `grep "millionth" data.txt` to find the line where `millionth` is present
pswd: VR1ljMayciFxbnUokuQmJFw6QC9VKtub
timetaken: 33s


# LEVEL 8 -> LEVEL 9
 - if you `cat data.txt` you see a bunch of lines
 - if you `sort data.txt` you see that the repeating lines are together but it's still a hassle to go thru each one of them to find the unique one
 - but in the `--help` section of the `uniq` command you see `-c` which gives the count
 - so, if we `sort data.txt | uniq -c` then we get the number of times each line repeats and we see only one line which repeats once, that is our password
pswd: EjmOSvuAu7sGAHqHVcBDPirRe9T03kxl
timetaken: 2m 29s


# LEVEL 9 -> LEVEL 10
 - if you do `whatis strings` you see: print the sequences of printable characters in files,
   so `strings` gives us human-readable strings.
 - we can grep the "==" symbol as it is preceded by *several* "=" characters
 - so if we do `strings data.txt | grep "==" ` we get: 
                                                       ========== the
                                                       [==p+
                                                       ========== password
                                                       Y========== is
                                                       ========== B0s2khmbT9u0geKuOoVGW3JZKhndE3BG
   so the password must be the last string of characters.

pswd: B0s2khmbT9u0geKuOoVGW3JZKhndE3BG
timetaken: 6m


# LEVEL 10 -> LEVEL 11
 - if I `cat data.txt  I see that there is only one line in the base64 encryption
 - in the `base64 --help` section we see a command `-d` which decodes the data,
   and since I only have 1 line, even if I decode the entire `data.txt` file, there is no harm.
 - on running `base64 -d data.txt` we get:  
                                          The password is pYfOY6HwUsDj5rL9UvyhU7MCmv8vN5Ro

pswd: pYfOY6HwUsDj5rL9UvyhU7MCmv8vN5Ro
timetaken: 3m 26s


# LEVEL 11 -> LEVEL 12
 - in all the commands given, if we run `whatis tr` we see: 
                                                        tr (1)               - Translate or delete characters
   which means that it's gonna let us translate one framework of letters to another when we define them as sets
 - in the `Implementation` heading of the Wikipedia page of ROT13 (Helpful Reading Material) we see that:
   we can define the sets for `tr` as: SET1 => 'A-Za-z' and SET2 => 'N-ZA-Mn-za-m'
 - we use this because we need characters from A to M to be printed as N to Z and characters from N to Z to be printed as A to M
 - we run `cat data.txt | tr 'A-Za-z' 'N-ZA-Mn-za-m' ` we get: 
                                                              The password is GROozWPO8QyN0mGrjUkID0WCYkZiQxrN

pswd: GROozWPO8QyN0mGrjUkID0WCYkZiQxrN
timetaken: 7m 46s


# LEVEL 12 -> LEVEL 13
 - `cat data.txt` to get some random hex dump and "data2.bin" mentioned
 - `whatis xxd` gives: make a hex dump or do the reverse.
 - we run the given command to make a directory: `mktemp -d`, it created: /tmp/tmp.7q38FtQY3M
 - we make a copy of "data.txt" in the folder created by `cp data.txt /tmp/tmp.7q38FtQY3M`
 - we go to that temp folder using `cd /tmp/tmp.7q38FtQY3M`
 - if we list files using `ls` we get: data.txt
 - `xxd -h` gives  us that `-r` can be used to reverse the compression
 - we do: `xxd -r data.txt > data` to store the reverted file in "data"
   since we don't know what exactly is the reverted data we do: `file data`
   we get:    data: gzip compressed data, was "data2.bin" 
 - if we do `gzip -h` we see that we can use `gzip -d` to decompress
 - we rename the data.gz  (file extension for gzip is .gz) file to newfile.gz to avoid loss of data
 - we decompress the newfile.gz by: `gzip -d newfile.gz`
 - when we list (`ls`) we get: data.txt newfile
 - if we run `file newfile` we get:    newfile: bzip2 compressed data
 - to decompress bzip2 files we can use `-d` (mentioned in `-h`)
 - we rename newfile as newfile.bz2 (file extension for bzip2 is .bz2) to avoid errors while decompressing
 - if we decompress it by: `bzip2 -d newfile.bz2` and list (`ls`) we get:    data.txt newfile
 - `file newfile`, returns:    newfile: gzip compressed data, was "data4.bin"
 - since this is now a gzip, we rename it to newfile.gz
   then we decompress using: `gzip -d newfile`
 - now if we run `file newfile` we get:    newfile: POSIX tar archive (GNU)
 - since this is a tar compressed file, we rename it to newfile.tar
 - in `tar --help` we see that to extract a tar file we run `tar -xf filename.tar`
   now we do: `tar -xf newfile.tar`, when we run `ls` after that we see:
                                                                        data.txt data5.bin newfile.tar

 - if we run `file data5.bin` we get:    data5.bin: POSIX tar archive (GNU)
 - since this is a tar file, we change the name of data5.bin to data5.tar
   now we extract data5.tar using `tar -xf data5.tar` and then `ls` to get:    data.txt  data5.tar  data6.bin  newfile.tar
 - we run `file data6.bin` to get:    data6.bin: bzip2 compressed data
 - we rename it to a .bz2 file and then decompress and the `ls` to get:    data.txt  data5.tar  data6  newfile.tar
 - if we `file data6` we get:    data6: POSIX tar archive (GNU)
 - so we rename it to `data6.tar` and then extract it and then `ls` to get:    data.txt  data5.tar  data6.tar  data8.bin  newfile.tar
 - we run `file data8.bin` to get:    data8.bin: gzip compressed data, was "data9.bin"
 - since it is gzip, we rename it to data8.gz and then decompress it
 - now if we `ls` we get:    data.txt  data5.tar  data6.tar  data8  newfile.tar
 - if we check the filetype of data8 using: `file data8` we get:
                                                                data8: ASCII text
                          (FINALLY YAY!)
 - we can now `cat data8` to get:
                                 The password is qQYQiHOBPR8zR61qxYqX45quvihF2uzk

pswd: qQYQiHOBPR8zR61qxYqX45quvihF2uzk
timetaken: 25m 23s


# LEVEL 13 -> LEVEL 14
 - if we `l -alps` we get:
                          -rw-r-----   1 bandit14 bandit13 2602 Jun 24 14:58 sshkey.private
 - we see that the USER can READ&WRITE, the GROUP can READ ONLY and OTHERS have NO PERMS 
   (according to the table in Wikipedia's articel on `chmod`)
 - since we only want the user `bandit14` to have access to the PRIVATE SSH Key and not others, 
   we change the mode to 600 using `chmod 600 sshkey.private`
 - in `man ssh` we get:
               -i  identity_file
                             Selects a file from which the identity (private key) for public key authentication is read.  You can
                             also specify a public key file to use the corresponding private key that is loaded  in  ssh-agent(1)
                             when  the  private  key file is not present locally.
 - but, it is mentioned in point 3 of the HINT file (`cat HINT`), we can not log into another level via localhost and since the SSH Key is private, we can not use band14@bandit.labs.overthewire.org
 - if we make a copy of this SSH Key in our own system, with our own protection, we can bypass the issue (is what I thought), so:
    `cat sshkey.private` to get the Key, copy it using `ctrl+shift+c` BUT if you create the file inside the bandit13 dir, as it says:
       `Could not create directory '/home/bandit13/.ssh' (Permission denied).`
 - after we copy the SSH Key(with the line above and below it), we exit bandit13 and make a new file in our system,
   `nano bandit14user.key` and paste the SSH Key in the text editor `nano`, save it and exit the editor
       - if we try to log into bandit14 with this user key, by `ssh -i bandit14user.key bandit14@bandit.labs.overthewire.org -p 2220 `,
         we get an error saying 
         WARNING: UNPROTECTED PRIVATE KEY FILE!
         Permissions 0644 for 'bandit14user.key' are too open.
         It is required that your private key files are NOT accessible by others.
       - so we need to do `chmod 600 bandit14user.key` to make sure it's only READ&WRITE for the USER, which is bandit14 here
 - now we can run `ssh -i bandit14user.key bandit14@bandit.labs.overthewire.org -p 2220 `, to successfully log into level 14 as bandit14
   we can confirm this using `whoami` which returns `bandit14` 
 - in the Level Goal, the address for the password is given as: /etc/bandit_pass/bandit14
   so we `cat /etc/bandit_pass/bandit14` to get:    aaWecNkG4FhxJQxz07uiwzVP6bJiYS6

pswd: aaWecNkG4FhxJQxz07uiwzVP6bJiYS65
timetaken: 36m 57s


# LEVEL 14 -> LEVEL 15
 - if we do `man nc` we get:
       It can open TCP connections, send UDP packets, listen on arbitrary TCP and UDP ports, do port scanning, and
       deal  with  both  IPv4 and IPv6.
 - and the syntax is: `nc [destination] [port] `
 - we run `nc localhost 30000` as mentioned in the Level Goal 
   and in the input line, we give the password of this level which is: aaWecNkG4FhxJQxz07uiwzVP6bJiYS65
 - it returns: 
               Correct!
               pbLYuZtTg4MgaqfJx8jbA9gKKGqM68A7

pswd: pbLYuZtTg4MgaqfJx8jbA9gKKGqM68A7
timetaken: 13m 47s


# LEVEL 15 -> LEVEL 16
 - in `man openssl` we see:
                           s_client
                                    This implements a generic SSL/TLS client which can establish a transparent  connection  to  a  remote  server
                                    speaking  SSL/TLS.
 - so we run `openssl s_client localhost:30001` as specified,
    in the input line, we give the password of this level which is: pbLYuZtTg4MgaqfJx8jbA9gKKGqM68A7
 - it returns: 
              Correct!
              kS0Hf0u5HiXFwKMKFqXvPdOTNGGa0X8V

pswd: kS0Hf0u5HiXFwKMKFqXvPdOTNGGa0X8V
timetaken: 8m 11s

